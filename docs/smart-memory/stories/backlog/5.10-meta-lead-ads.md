---
title: "Story 5.10: Integração Meta Lead Ads (formulário instantâneo) + opener outbound"
type: story
status: backlog
epic: 5
wave: P2
complexity: L
god-node: true
mode: pre-flight
agent: crm-architect
created: 2026-06-25
updated: 2026-06-25
tags: [story, portal, meta-lead-ads, leadgen, outbound, atribuicao]
related: ["[[../../decisions/ADR-004-canal-whatsapp-qr-vs-make]]", "[[5.8-evolution-self-hosted]]", "[[5.6-modulo-metricas-bi]]", "[[../../agents/data-engineer/schema]]"]
---

# Story 5.10: Integração Meta Lead Ads (formulário instantâneo) + opener outbound

## Objetivo
Receber leads do **formulário instantâneo do Meta (Lead Ads)** via webhook `leadgen`, criar o lead no CRM e **disparar a 1ª mensagem (outbound-first) no WhatsApp** via Evolution, transferindo o lead para o fluxo inbound do agente quando ele responder.

## Fluxo (novo ingresso, distinto do inbound)
```
Form Meta preenchido → webhook leadgen (/api/leadgen) → busca dados via Graph API
  → cria lead (com atribuição: form/anúncio/campanha) → opener outbound via Evolution
  → lead responde → /api/webhook (Evolution) → handleInbound + agente (fluxo existente)
```

## Acceptance Criteria
- [ ] AC1: Rota `/api/leadgen` assina o webhook `leadgen` do Meta: responde ao **verify token** (GET) e valida a **assinatura** (`X-Hub-Signature-256`) nos POSTs.
- [ ] AC2: Ao receber um `leadgen_id`, busca os dados do lead via **Graph API** (nome, telefone, respostas do form) com o token da página.
- [ ] AC3: Cria/atualiza o lead no CRM (reusa `getOrCreateLead` por telefone normalizado E.164); idempotente por `leadgen_id` (não duplica em reentrega do webhook).
- [ ] AC4: **Captura de atribuição:** form id, `ad_id`/`campaign_id`/`adset` gravados no lead (campos de origem — coordenar com a crm-data) para o BI ([[5.6-modulo-metricas-bi]]).
- [ ] AC5: **Opener outbound-first:** dispara uma 1ª mensagem natural/**configurável por serviço** via Evolution (`sendText`); registra como `out`; o lead entra em `em_atendimento`. Quando responder, segue o fluxo inbound normal.
- [ ] AC6: **Velocidade:** o opener sai em segundos do `leadgen` (lead quente) — processamento síncrono ou disparo imediato; medir latência leadgen→opener.
- [ ] AC7: **Anti-ban / rate limiting:** envio controlado (sem blast), respeitando limites; documentar o cuidado (opt-in via form reduz risco, não elimina). Falha de envio não perde o lead (retry/log).

## Escopo

**IN:**
- Webhook `leadgen` (verify + assinatura), fetch Graph API, criação de lead com atribuição, opener outbound configurável, idempotência por `leadgen_id`, rate limiting básico.

**OUT:**
- Telas de BI de atribuição (consome estes dados — Story 5.6).
- Editor visual de templates de opener (v2 — aqui basta configurável por serviço/env).
- O canal de respostas (já é o `/api/webhook` da Evolution).

## Contexto Técnico
**God node:** novo ingresso que cria leads e dispara envio — toca `crm/leads.ts` (criação/atribuição), `whatsapp/evolution.ts` (`sendText` do opener), `types.ts` (campos de origem) e adiciona a rota `/api/leadgen`. **Dois ingressos distintos** (documentar): `/api/leadgen` (novos leads do Meta, outbound-first) vs `/api/webhook` (respostas do lead, inbound). O **opener livre exige Evolution** — a Cloud API oficial pediria template aprovado; reforça [[../../decisions/ADR-004-canal-whatsapp-qr-vs-make]]. Depende de 5.8 (Evolution hospedada para o envio).

## Dev Agent Record

| Campo      | Valor |
|---         |---|
| Agente     | — |
| Iniciado   | — |
| Concluído  | — |
| Branch     | — |

## File List
<!-- Dev preenche ao concluir -->

## QA Results
<!-- QA preenche ao revisar -->
