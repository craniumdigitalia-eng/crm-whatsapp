---
title: "Story 3.2: Scaffold serverless — rotas Express para funções /api"
type: story
status: backlog
epic: 3
wave: 2
complexity: L
god-node: false
mode: interactive
agent: crm-architect
created: 2026-06-25
updated: 2026-06-25
tags: [story, vercel, serverless, scaffold]
related: ["[[2.2-rewrite-persistencia-supabase]]", "[[3.3-api-webhook-idempotente]]", "[[3.4-api-cron-followup]]", "[[project/architecture]]"]
---

# Story 3.2: Scaffold serverless — rotas Express para funções /api

## Objetivo
Reestruturar a aplicação Express de processo longo em funções serverless da Vercel, criando a base (`/api`, `vercel.json`) sobre a qual webhook e cron serão implementados.

## Acceptance Criteria
- [x] AC1: Estrutura `/api` criada com funções serverless; `src/index.ts` (app.listen) deixa de ser o entrypoint de produção.
- [x] AC2: As rotas do dashboard (`/api/leads`, `/api/leads/:id`, reply/status/takeover/release/edit) viram funções serverless equivalentes, consumindo o repositório async (Story 2.2).
- [x] AC3: `vercel.json` configurado (funções + placeholder do cron); `health` endpoint disponível.
- [x] AC4: Nenhum estado em memória entre invocações (sem `node-cron`, sem singletons mutáveis); client Supabase inicializado por invocação ou reusado de forma stateless-safe.
- [ ] AC5: Deploy de preview na Vercel responde às rotas de leitura (`GET /api/leads`) contra o Supabase.

## Escopo

**IN:**
- Migrar as rotas do `routes/api.ts` para funções serverless.
- `vercel.json`, estrutura de pastas `/api`, variáveis de ambiente na Vercel.

**OUT:**
- `/api/webhook` (Story 3.3) e `/api/cron/followup` (Story 3.4) — só o esqueleto/roteamento aqui.
- Migração para Next.js App Router (decisão de ADR; esta story usa funções `/api` puras).

## Contexto Técnico
Risco #2 da arquitetura: o fire-and-forget do webhook não sobrevive — esta story estabelece o padrão de função stateless. Depende de 2.2 (repositório async). Ver [[project/architecture]].

## Dev Agent Record

| Campo      | Valor |
|---         |---|
| Agente     | Orion (crm-backend) |
| Iniciado   | 2026-06-25 |
| Concluído  | 2026-06-25 |
| Branch     | feat/3-2-scaffold-serverless-vercel |

## File List
- `api/_lib/supabase.ts` — re-export do client Supabase (stateless-safe)
- `api/_lib/response.ts` — helpers ok/notFound/badRequest/serverError
- `api/_lib/validate.ts` — guardMethod/requireId/validateStatus
- `api/health.ts` — GET /api/health
- `api/leads/index.ts` — GET /api/leads
- `api/leads/[id]/index.ts` — GET /api/leads/:id
- `api/leads/[id]/reply.ts` — POST /api/leads/:id/reply
- `api/leads/[id]/status.ts` — POST /api/leads/:id/status
- `api/leads/[id]/takeover.ts` — POST /api/leads/:id/takeover
- `api/leads/[id]/release.ts` — POST /api/leads/:id/release
- `api/leads/[id]/edit.ts` — POST /api/leads/:id/edit
- `api/webhook.ts` — PLACEHOLDER 3.3 (501)
- `api/cron/followup.ts` — PLACEHOLDER 3.4 (501)
- `vercel.json` — functions runtime + cron config
- `src/index.ts` — comentário dev-only adicionado
- `package.json` / `package-lock.json` — @vercel/node adicionado como devDep

## QA Results

**VEREDICTO: ✅ PASS** (1 concern a verificar antes do deploy; 2 nits) — Tessera (crm-qa) — 2026-06-25
Checklist: 8/8 verificados | `npx tsc --noEmit`: EXIT=0 ✅

Respostas aos pontos do gate:
1. **7 rotas fiéis** ✅ — comparadas 1:1 com `routes/api.ts` (mesma lógica, delegando ao `src/`). Sem regressão; endurecidas com `guardMethod` (405) e `requireId` (400), necessários no serverless. `public/app.js` casa com os paths.
2. **`requireId`** ✅ — narrowing `typeof id === "string" && length>0`; `string[]` → null → 400 (rejeita defensivamente).
3. **guardMethod + try/catch** ✅ — todos os 7 handlers; `serverError` loga server-side e devolve só `e.message` (sem stack).
4. **`_lib/supabase.ts` stateless-safe** ✅ — singleton de `src/db.ts` (`persistSession:false`), sem pool mutável; nenhum handler importa scheduler/node-cron.
5. **vercel.json** ✅ (com ressalva) — runtime `@vercel/node@5` válido; `_lib/` excluído por prefixo `_`; cron path casa; `public/` static. Exceto o concern do `comment` abaixo.
6. **Placeholders 501** ✅ — claros, documentados, não quebram.
7. AC5 (deploy) pendente (setup Vercel do usuário, fora do gate); 501s não penalizados.
8. `tsc` EXIT=0 ✅.

Observações:
- **[CONCERN — antes do deploy]** `comment` em `crons[]` do `vercel.json`: schema do Vercel aceita só `path`+`schedule`; propriedade extra pode falhar a validação no deploy ("additional properties") e quebrar o AC5. Mover/remover antes do 1º deploy. Falha barulhenta e trivial; não-bloqueante p/ o scaffold.
- **[NIT]** Doc do placeholder `webhook.ts` diz `{...external_id}`, mas a 3.1 definiu `{...id}`. A 3.3 deve usar `parseMakeWebhook` (campo `id`). Alinhar.
- **[NIT]** `health.ts` sem `guardMethod` (responde a qualquer método) — trivial.

Detalhe completo em `docs/smart-memory/agents/qa/results.md`.
